import React, { useEffect } from 'react'
import { useParams } from 'react-router';

export const FullBook = ({ books }) => {
    const { id } = useParams();

    useEffect(() => {

    }, [])


    return (
        <div>

        </div>
    )
}
